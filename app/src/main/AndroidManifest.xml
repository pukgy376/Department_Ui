<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.department_ui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Edit_profile_department"></activity>
        <activity android:name=".Profile_Department" />
        <activity android:name=".Other" />
        <activity android:name=".Comfirm_Help_Show_Post" />
        <activity android:name=".Confirm_Help_Post" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Show_Post_Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>